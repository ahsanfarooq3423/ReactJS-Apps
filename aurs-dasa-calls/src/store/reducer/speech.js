
const initialState = {
    setup : 'this is for the speech',
    fileData : null
}


const reducer = (state = initialState, action) => {
    switch(action.type) {
        default : 
            return {
                ...state
            }
    }
}

export default reducer;